const template = `
  <div class="profile-data">
    <span class="profile-data__title">{{ title }}</span>
    <span class="profile-data__value">{{ value }}</span>
  </div>
`;

export default template;
