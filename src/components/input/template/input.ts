const template = `
  <input
      class="input"
      id="{{ id }}"
      type="{{ type }}" 
      name="{{ name }}"
      placeholder="{{ placeholder }}"
      value="{{ value }}" 
  >
`;

export default template;
